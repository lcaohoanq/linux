- Thunar
- Dolphin
- Nautilus (default file manager for GNOME)

# Thunar

- Arch

```bash
yay -S thunar tumbler ffmpegthumbnailer tumbler-plugins-extra xfce4-settings thunar-archive-plugin
```

- Debian

```bash
sudo apt install thunar tumbler tumbler-plugins-extra ffmpegthumbnailer xfce4-settings thunar-archive-plugin file-roller
```

# Dolphin

- Arch

```bash
sudo pacman -S dolphin
```

- Debian

```bash
sudo apt install dolphin
```
